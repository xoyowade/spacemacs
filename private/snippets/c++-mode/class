# name: class ... { ... }
# key: class
# note: DISALLOW_COPY_AND_ASSIGN(${1:$(yas-substr yas-text "[^: ]*")});
# --
class ${1:Name} {
public:
    ${1:$(yas-substr yas-text "[^: ]*")}($2);
    virtual ~${1:$(yas-substr yas-text "[^: ]*")}();
private:
};
